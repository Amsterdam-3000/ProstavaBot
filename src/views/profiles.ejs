/<%= COMMAND.PROFILE %> <%= LocaleUtils.getCommandPlaceholder(i18n, COMMAND.PROFILE) %> - <%= LocaleUtils.getCommandText(i18n, COMMAND.PROFILE_SHOW) %>
<% users.forEach(function(user){ %><%= user.personal_data.emoji %> <%= user.personal_data.name %>
<% }); %>