/<%= COMMAND.PROFILES %> <%= LocaleUtils.getCommandPlaceholder(i18n, COMMAND.PROFILES) %> - <%= LocaleUtils.getCommandText(i18n, COMMAND.PROFILES_USER) %>
<% users.forEach(function(user){ %><%= user.personal_data.emoji %> <%= user.personal_data.name %>
<% }); %>