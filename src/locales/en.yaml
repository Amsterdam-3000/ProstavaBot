reply:
    not_group: "${error_code} I only work in groups!"
    not_admin: "${error_code} You are not admin in this group!"
    app_wrong: "${error_code} Something went wrong!"
    are_going: "${error_code} Where are you going!?"
    send_emoji: "${default_code} Send me Your emoji"
    send_birthday: "${default_code} Send me Your date of birth (YYYY-MM-DD)"
command:
    start: "${command_code} start Prostava"
    help: "${command_code} show Prostava help"
    settings: "${command_code} change Prostava settings"
    profile: "${command_code} complete Your profile ${username}"
    prostava: "${command_code} announce new prostava"
    search: "${command_code} search for prostavas"
    language: "${command_code} change Prostava language" 
command_group:
    bot: "Bot"
    user: "User"
    prostava: "Prostava"
header:
    help: "I can help you control prostavas. Here are my commands:"
action:
    show_languages: "Language: ${language}"
    set_emoji: "Emoji: ${emoji}"
    set_birthday: "Birthday: ${birthday}"
    back: "${default_code} Back"
